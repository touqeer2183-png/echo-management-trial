function workTable(rows,actions=true){return `<div class="table"><table><thead><tr><th>Token / Date</th><th>Patient</th><th>Registration No.</th><th>Created by</th>${actions?'<th></th>':''}</tr></thead><tbody>${rows.map(v=>`<tr><td><b class="token">${token(v)}</b><br><small>${fmtDate(v.token_date || v.created)}</small></td><td><b>${esc(v.patient_name)}</b><br><small>${esc(v.age||'')} · ${esc(v.sex||'')}</small></td><td>${esc(v.reg_no)}</td><td>${esc(v.creator)}</td>${actions?`<td>${user.role==='operator'||user.role==='admin'?`<button data-start="${v.id}">Start Echo</button>`:`<button class="light" data-reprint="${v.id}">Reprint</button>`}</td>`:''}</tr>`).join('')}</tbody></table>${rows.length?'':'<div class="empty">No pending patients.</div>'}</div>`}
function patientRows(rows){return `<div class="table"><table><thead><tr><th>Registration No.</th><th>Patient</th><th>CNIC</th><th>Phone</th><th>Last Echo</th><th></th></tr></thead><tbody>${rows.map(p=>`<tr><td><b>${esc(p.reg_no)}</b></td><td>${esc(p.name)}<br><small>${esc(p.relation_type)} ${esc(p.relation_name)}</small></td><td>${esc(p.identity||p.guardian_identity)}</td><td>${esc(p.phone)}</td><td>${fmtDate(p.last_echo)}</td><td><button class="light" data-patient="${p.id}">Open</button></td></tr>`).join('')}</tbody></table></div>`}
