function auth(setup){$('#app').innerHTML=`<main class="auth"><section class="login"><div class="logo">AHC</div><p>AFZAL HEART CENTRE</p><h1>Echo Management System</h1><form id="login">${setup?input('name','Full name','text','',true):''}${input('username','Username','text','',true)}${input('password','Password','password','',true)}<button>${setup?'Create administrator':'Sign in'}</button></form>${companyCredit()}</section></main>`;$('#login').onsubmit=async e=>{e.preventDefault();try{let d=fd(e.target);if(setup)await api('setup',d);await api('login',d);boot()}catch(x){toast(x.message)}}}
